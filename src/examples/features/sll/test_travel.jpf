@include=./example.jpf

concolic.method=travel
costar.data=data features_sll_Node {features_sll_Node next}
costar.predicate=pred sll(root) == root = null || root::features_sll_Node<next> * sll(next)
costar.precondition=pre travel == sll(root)
