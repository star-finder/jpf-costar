@include=./example.jpf

concolic.method=travel
costar.data=data Node {Node next}
costar.predicate=pred sll(root) == root = null || root::Node<next> * sll(next)
costar.precondition=pre travel == sll(root)