@include=./example.jpf

target.args=withDecrementExact
concolic.method=withDecrementExact

costar.data=data random_sll_Node {int elem; random_sll_Node next}
costar.predicate=pred sll(root) == root = null || root::random_sll_Node<elem,next> * sll(next)

costar.precondition=pre withDecrementExact == sll(root)
costar.root=root

star.max_depth=0